sim_rate          = 312
n_seconds         = 5
gravity           = (0.0, -9.81, 0.0)
target_compliance = 0.0001
compliance        = 0.00011
target_offset     = (0.0, 0.0, 0.0)
offset            = (0.0, 0.0, 0.0)
ground_ori        = (0.0, -6.0, 0.0)
ground_normal     = (0.0, 1.0, 0.0)
export_obj        = false
fps               = 24

# objects:
#   - chain(length)
#   - cloth(width, height)
obj = chain(10)

# experiments:
#   - compliance_gradient
#   - x0_gradient
#   - single_step_jacobian(step)
#   - compliance_optimization(lr, iters)
experiment = compliance_optimization(1e-10, 1000)

# loss:
#   - mse_final_position
#   - mse_full_trajectory
#   - mse_frames_trajectory(fps)
loss = mse_frames_trajectory(24)