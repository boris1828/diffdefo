sim_rate  = 312
n_seconds = 4

gravity = (0.0, -9.81, 0.0)

target_compliance = 0.001
compliance        = 0.0005
target_offset     = (0.0, 0.0, 0.0)
offset            = (0.0, 0.0, 0.0)

# colliders:
#   - halfspace(origin: vec3, normal: vec3)
#   - sphere(center: vec3, radius: scalar)
colliders = [ sphere((4.5, -8.0, 0.0), 3.0) ]
# 
# halfspace((0.0, -7.0, 0.0), (0.0, 1.0, 0.0))

export_obj = true
fps        = 24

# objects:
#   - chain(length)
#   - cloth(width, height)
obj = cloth(10, 10)

# experiments:
#   - forward_simulation
#   - compliance_gradient
#   - x0_gradient
#   - single_step_jacobian(step)
#   - compliance_optimization(iters)
experiment = compliance_gradient

# optimizer (required for optimization experiments):
#   - GD(lr)
#   - momentum(lr, beta=0.9)
#   - ADAM(lr, beta1=0.9, beta2=0.999, epsilon=1e-8)
optimizer = momentum(2e-9, 0.8)

# loss:
#   - mse_final_position
#   - mse_full_trajectory
#   - mse_frames_trajectory(fps)
loss = mse_frames_trajectory(24)